
/*
let document = document.getElementById("document");
console.log(document);


window.onload = function() {
    let document = document.getElementById("document");
    console.log(document);
    //console.log(styleE.innerHTML);
    document.onload = function() {
        console.log("document onload");
        //let doc = iframe.contentDocument;
        //console.log(doc);
        //doc.body.innerHTML = doc.body.innerHTML + '<style>._4-u2 {border:none;}</style>';
    }
    
    
}
*/
/*
let frames = document.getElementsByClassName("reduceHeight");

function reduceFrame() {
    let inW = window.innerWidth;
    for(frame of frames) {
        if(inW >= 1200) {
            frame.style.height = "725px";
        }else {
            frame.style.height = "320px"; 
        }
    }
    
}
window.addEventListener('resize', reduceFrame);
*/